import styles from "./sponsors.module.css"
const Sponsors = () => {
    return (
        <section className={styles.sponsors}>
            <h2>Sponsors</h2>
            <p>PLACEHOLDER</p>
        </section>
    );
};

export default Sponsors;
